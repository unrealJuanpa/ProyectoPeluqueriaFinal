@model IEnumerable<ProyectoPeluqueriaFinal.Models.Razon_retiro_producto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_razon_retiro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion_razon_retiro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_razon_retiro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion_razon_retiro)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_retiro_producto }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_retiro_producto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_retiro_producto })
        </td>
    </tr>
}

</table>
