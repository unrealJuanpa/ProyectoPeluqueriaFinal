@model IEnumerable<ProyectoPeluqueriaFinal.Models.Rol_empleado>

@{
    ViewBag.Title = "Index";
}

<br /><br /><br /><br />

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_rol_empleado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nivel_acceso)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_rol_empleado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nivel_acceso)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_rol_empleado }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_rol_empleado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_rol_empleado })
        </td>
    </tr>
}

</table>
