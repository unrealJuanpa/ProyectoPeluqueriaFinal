@model IEnumerable<ProyectoPeluqueriaFinal.Models.Insumo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_insumo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripción)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_insumo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripción)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_insumo }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_insumo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_insumo })
        </td>
    </tr>
}

</table>
